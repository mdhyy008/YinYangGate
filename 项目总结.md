# 项目总结

> 生成时间：2026-08-14
> 由 Claude Code project-summary skill 自动生成

---

## 一、项目概述

| 项目 | 内容 |
|------|------|
| **项目名称** | 阴阳门（YinYangGate） |
| **项目用途** | Android 应用冻结/解冻管理工具（类似「冰箱」） |
| **项目类型** | Android 移动应用 |
| **技术栈** | Kotlin、Jetpack Compose（Material3）、Shizuku、Gradle |
| **构建工具** | Android Gradle Plugin 9.2.1 + Gradle 9.4.1 |

「阴阳门」是一款管理 Android 应用**冻结 / 解冻**状态的应用管理工具。设计灵感取自「阴阳」二元思想：未冻结的应用在**阳面**（明亮主题）、已冻结的应用在**阴面**（暗色主题），通过 180° 翻转动画在两面间切换，像翻硬币一样管理应用。冻结通过系统 `pm disable-user` / `pm enable` 命令实现，适合隐藏应用、省电、防孩子乱玩等场景。应用目前已是 v1.0 状态，已编写完整的用户使用说明文档，并持续迭代新增功能。

---

## 二、技术栈详情

| 类别 | 技术 |
|------|------|
| 语言 | Kotlin 2.2.10（Java 11 兼容） |
| UI 框架 | Jetpack Compose（BOM 2026.02.01，Material3，material-icons-extended） |
| 权限通道 | Shizuku 12.1.0（dev.rikka.shizuku api + provider） |
| 数据存储 | SharedPreferences（JSON 字符串）+ 应用文件目录（图标/快照缓存） |
| 并发 | kotlinx.coroutines |
| 系统 API | API 24（Android 7.0）~ API 36 |
| 构建 | AGP 9.2.1、Gradle 9.4.1（Wrapper）、JDK 21 toolchain |
| 测试 | JUnit 4、Espresso（仅示例测试） |

- **compileSdk**：36（API 36.1）　**minSdk**：24　**targetSdk**：36
- **applicationId / namespace**：`cn.mdhyy.yinyanggate`
- **版本**：1.0（versionCode 1）

---

## 三、项目结构

```
YinYangGate/
├── app/                                   # Android 应用模块
│   ├── build/                             # 构建产物（debug/release APK 均已生成）
│   └── src/
│       ├── main/
│       │   ├── AndroidManifest.xml        # 权限、ShizukuProvider、AdminReceiver、MainActivity
│       │   ├── java/cn/mdhyy/yinyanggate/
│       │   │   ├── MainActivity.kt         # 入口；三屏导航（主/记录/设置）；翻转动画驱动与主题渐变
│       │   │   ├── admin/
│       │   │   │   └── AdminReceiver.kt     # DPM 设备管理员 Receiver（空实现）
│       │   │   ├── channel/               # 冻结通道层
│       │   │   │   ├── ChannelManager.kt    # 通道列表、偏好选择与记忆
│       │   │   │   ├── FreezeChannels.kt    # 5 种通道实现（Root/Shizuku/DPM/ADB/PC）
│       │   │   │   └── PcClientManager.kt   # 局域网 UDP 与电脑客户端通信（发现/推送/冻结中继）
│       │   │   ├── data/                  # 数据层
│       │   │   │   ├── AppInfo.kt           # 应用数据模型 + 图标位图转换
│       │   │   │   ├── FreezeRecord.kt      # 操作记录模型
│       │   │   │   └── AppRepository.kt     # 应用加载、快照缓存、冻结/解冻、记录存储
│       │   │   └── ui/                    # Compose 界面层
│       │   │       ├── YinYangScreen.kt     # 主界面（翻转网格、多选批量、ADB 引导、功能菜单）
│       │   │       ├── ChannelScreen.kt     # 设置页（通用开关、权限通道、关于）
│       │   │       ├── HistoryScreen.kt     # 操作记录页
│       │   │       ├── CommonTopBar.kt      # 共用顶栏
│       │   │       └── theme/               # Color/Theme/Type（主题随翻转进度插值渐变）
│       │   └── res/                        # 资源：图标（mipmap）、strings、themes、xml
│       ├── androidTest/                   # 仪器测试（仅示例，无实际用例）
│       └── test/                          # 单元测试（仅示例）
├── gradle/
│   ├── wrapper/                          # Gradle Wrapper（9.4.1）
│   └── libs.versions.toml                # 依赖版本目录（centralized version catalog）
├── build.gradle.kts                      # 顶层构建脚本
├── settings.gradle.kts                   # 仓库配置、模块声明
├── gradle.properties                     # JVM 参数（-Xmx2048m）
├── local.properties                      # 本地 SDK 路径（已被 .gitignore 忽略）
├── icon_backup/                          # 应用图标备份（ic_launcher XML 源文件）
├── TODO.md                               # 未完成任务清单
├── TODO-电脑客户端.md                     # 电脑客户端模式需求规划
└── 阴阳门使用说明.md                      # 用户使用说明（v1.0）
```

---

## 四、已开发功能

### ✅ 已完成功能

1. **阴阳双面应用网格 + 180° 翻转动画** — 主界面按冻结状态分阳面/阴面，顶栏按钮点击或左右滑动（需设置开启）触发 180° 翻转，主题颜色随翻转进度 `flipProgress` 线性插值渐变（`ui/theme/Theme.kt`、`ui/YinYangScreen.kt`）。
2. **单个应用冻结 / 解冻** — 点击应用弹确认框，系统应用额外红色警告；操作经通道执行并写入记录（`ui/YinYangScreen.kt:938`、`data/AppRepository.kt:168`）。
3. **批量冻结 / 解冻（多选模式）** — 长按进入多选、高亮选中、底部批量栏（冻结/解冻/切换状态）、全选/反选当前面，带进度与二次确认（`ui/YinYangScreen.kt:993`）。
4. **5 种权限通道** — Root（`su`）、Shizuku（`Shizuku.newProcess`）、DPM（设备所有者）、ADB 半手动（弹命令引导）、PC 电脑客户端（局域网 UDP 中继），选择立即生效且记忆，不做自动回退（`channel/FreezeChannels.kt`、`channel/ChannelManager.kt`）。
5. **ADB 手动命令引导** — 自动生成 `adb shell pm disable-user` / `pm enable` 命令，支持逐条/全部复制，点「我已执行」后轮询验证冻结状态（`ui/YinYangScreen.kt:1329`）。
6. **操作记录页** — 冻结/解冻全记录，含应用、时间、通道、动作徽标，倒序展示，SharedPreferences 持久化（`ui/HistoryScreen.kt`、`data/AppRepository.kt:189`）。
7. **应用列表快照缓存** — 首次加载全量扫描并缓存图标 + JSON 元数据，后续启动先出快照快速上屏，已安装集合变化时全量刷新、否则仅轻量校正冻结状态（`data/AppRepository.kt:62`、`MainActivity.kt:110`）。
8. **电脑客户端（手机端）** — UDP 广播发现电脑、分片推送应用列表（图标色块/包名/名称/冻结态）、转发冻结指令并等待回执；固定端口手机 45678 / 电脑 45679（`channel/PcClientManager.kt`）。
9. **规则清单导出 / 批处理** — 多选导出为 `yinyang_rules` JSON 到剪贴板，功能菜单「清单批处理」粘贴导入后批量冻结/解冻（`ui/YinYangScreen.kt:342,1318`）。
10. **功能菜单** — 长按顶栏面切换按钮弹出：清单批处理、清单库、手动刷新；带 Dock 弹出动画（`ui/YinYangScreen.kt:1118`）。
11. **设置页** — 左右滑动切换面、显示系统应用、切换按钮置底、权限模式选择、帮助与关于（`ui/ChannelScreen.kt`）。
12. **系统应用标识** — 卡片右上角红点标识系统应用，操作前强警告。
13. **其它细节** — 旋转屏幕保持状态（`configChanges`）、退出确认弹窗、自绘顶部 Toast、搜索框（当前隐藏）、主界面返回键多选优先退出。

### 🔄 开发中功能

1. **电脑客户端 ADB 连接模式（端到端）** — 手机端 PC 通道代码已完成（`PcClientManager` + `PcChannel`），但**配套的 Windows 桌面客户端尚未开发**，功能无法闭环。详见 [TODO-电脑客户端.md](TODO-电脑客户端.md)。
2. **清单库** — 功能菜单「清单库」目前跳转占位网址 `https://github.com`（`ui/YinYangScreen.kt:1295` 注释「后续替换为真实页面」），未实现云端规则库。

### 📋 计划中功能

1. **无线调试配对协议（暂缓）** — 应用内输入配对码直连本机 adbd 执行 pm 命令，需实现 AOSP 无线调试配对协议（X25519 ECDH / scrypt / SP800-56A KDF / AES-256-GCM / RSA 认证）。TODO.md 提及的 `AdbWirelessClient.pair()` 占位类**在当前源码中不存在**，设置页显示「开发中，敬请期待」。
2. **云端规则库同步** — 使用说明中提到的未来规划：冻结规则导出 + 云端多设备同步分享。

---

## 五、开发进度

| 维度 | 状态 |
|------|------|
| **开发阶段** | 中后期（v1.0 已发布，配套使用说明，仍在迭代新功能） |
| **最近活跃** | 2026-08-14（最近一次 debug APK 构建） |
| **分支情况** | **非 git 仓库**（无版本控制） |
| **代码规模** | 16 个 Kotlin 源文件，主源码约 3,600 行 |
| **TODO 数量** | 源码内 1 处（模板注释）；TODO.md 记录 2 大项未完成功能 |

**开发状态补充**：

- 应用已完成 v1.0 并写了完整的《阴阳门使用说明.md》，说明文档与代码存在**轻微脱节**：文档仍描述设置页有「规则库」占位入口（当前代码中已移除），未提及已实现的 PC 通道与清单批处理。
- debug 与 release APK 均已成功构建（`app/build/outputs/apk/`）。
- release 构建暂用 debug 签名、未开启 minify（代码注释注明「临时用 debug 签名，便于验证 AOT 对启动卡顿的改善；正式发布需换成正式签名」）。
- 项目有较强的性能优化痕迹：快照缓存、图标位图后台一次性转换、组合函数拆分控制 JIT 编译单元、`MainActivity` 内大量 `YinYangPerf` 性能日志。

---

## 六、提交历史概览

```
当前项目不是 git 仓库（无 .git 目录），无法提供提交历史。
开发迭代时间线（依据文件修改时间与文档）：
- 2026-08-07  项目创建，编写 v1.0 使用说明、TODO 清单
- 2026-08-14  最近一次代码改动与 debug APK 构建
```

---

## 七、接手指南

### 环境要求
- **JDK**：21（Gradle daemon toolchain，见 `gradle/gradle-daemon-jvm.properties`）
- **Android SDK**：compileSdk 36 需较新 SDK Platform；`local.properties` 已指向 `C:\Users\JY-910\AppData\Local\Android\Sdk`
- **Gradle**：使用项目自带 Wrapper（9.4.1，已缓存，无需下载）

### 启动方式
```bash
# Windows（项目根目录）
gradlew.bat :app:assembleDebug     # 构建 debug APK
gradlew.bat :app:installDebug      # 安装到已连接设备
```
- 请使用项目**已有的 Gradle 版本**（Wrapper 9.4.1），避免自动下载新版本。

### 重要目录说明
- `app/src/main/java/cn/mdhyy/yinyanggate/channel/` — 冻结通道核心，扩展新通道需实现 `FreezeChannel` 接口并注册进 `ChannelManager.channels`。
- `app/src/main/java/cn/mdhyy/yinyanggate/data/AppRepository.kt` — 数据中枢：应用加载、快照缓存、冻结/解冻、记录持久化。
- `app/src/main/java/cn/mdhyy/yinyanggate/ui/YinYangScreen.kt` — 主界面（1515 行，最大文件），多数交互逻辑集中于此。
- `app/src/main/java/cn/mdhyy/yinyanggate/channel/PcClientManager.kt` — 局域网 UDP 协议，协议消息类型：`DISCOVER` / `DISCOVER_REPLY` / `REQUEST_LIST` / `APP_LIST_START` / `APP_LIST_FRAG` / `APP_LIST_END` / `FREEZE` / `FREEZE_RESULT`。
- `gradle/libs.versions.toml` — 集中管理全部依赖版本。

### 注意事项
- **非 git 仓库**：目前没有版本控制，改动无法回溯，建议尽快 `git init` 并提交基线。
- **PC 通道依赖 Windows 客户端**：`PcClientManager` 只是手机端，需配套电脑端才能工作；协议未加密鉴权，局域网内任何设备回复 `DISCOVER_REPLY` 即可伪装成电脑并获取应用列表、androidId。
- **通道选择不做回退**：选哪个通道就用哪个，不可用则操作直接报错（设计如此）。
- **冻结系统应用有风险**：界面有红色警告，但风险由用户承担。
- **ADB 通道是半手动**：只生成命令、由用户在电脑上执行后点「我已执行」验证。
- **文档可能滞后**：《阴阳门使用说明.md》与 TODO 清单以代码实际为准。

---

## 八、项目缺陷与改进建议

> 以下缺陷由 Claude Code 自动扫描检出，结合人工判断进行修复决策。

### 🔴 高危问题

1. **无版本控制（非 git 仓库）** — 项目没有任何源码版本管理，改动无法回退、无历史记录、无法协作或备份。**修复建议**：立即 `git init` 添加 `.gitignore`（已存在），提交当前可用的 v1.0 基线，之后每次功能完成即提交。

### 🟡 中危问题

1. **PC 通道局域网 UDP 无鉴权与加密** — `PcClientManager` 向 `255.255.255.255` 广播 `DISCOVER`，任何局域网设备回复 `DISCOVER_REPLY` 即可被当作「电脑」，随后可接收完整应用列表（包名、名称、色块），并在 `FREEZE` 指令中获取明文 `androidId`（`PcClientManager.kt:204,315`）。**修复建议**：参考 TODO-电脑客户端.md 中「鉴权/安全」待定项，引入一次性 PIN/二维码配对与对称加密（手机与电脑各持有配对密钥后再通信），至少对 `androidId` 这类敏感字段脱敏或去掉。

2. **Root 通道命令字符串拼接存在注入面** — `RootChannel.freeze/unfreeze` 用 `su -c "pm disable-user --user 0 $packageName"` 拼接 shell 命令（`FreezeChannels.kt:42,45`），与 `ShizukuChannel` 传参数数组的写法不一致。当前 `packageName` 来自 `PackageManager` 的已安装应用，实际风险低，但若未来接入任意字符串来源（规则清单、外部输入）即成注入点。**修复建议**：统一改为参数数组方式，或对包名做白名单校验（`^[a-zA-Z0-9_.]+$`）。

3. **无有效测试覆盖** — 仅存在模板示例测试（`ExampleUnitTest` / `ExampleInstrumentedTest`，无实际断言业务），通道逻辑、规则解析 `parseRuleList`、快照/记录序列化均无测试。**修复建议**：为 `data/AppRepository` 的快照与记录读写、`ui/YinYangScreen.kt:1318` 的规则 JSON 解析、各通道的 `isAvailable()` 判定补充 JVM 单元测试。

### 🟢 低危/建议

1. **`YinYangScreen.kt` 达 1515 行、`ChannelScreen.kt` 649 行** — 文件过大，部分可复用的对话框/组件混在一起。**修复建议**：将 ADB 引导、批处理、功能菜单等对话框拆分为独立文件。
2. **`GlobalScope` 使用** — `PcClientManager` 多处 `GlobalScope.launch`（`PcClientManager.kt:115,132,255,264`），协程生命周期不受控，可能泄漏。**修复建议**：改为注入 `CoroutineScope`（如应用级 `SupervisorJob`）统一管理。
3. **release 构建未加固** — `isMinifyEnabled = false` 且用 debug 签名（`app/build.gradle.kts:26-32`）。**修复建议**：正式发布前开启 R8 minify、配置正式签名，并处理相应的 proguard 规则。
4. **大量性能调试日志残留** — `MainActivity` / `PcClientManager` / `AppRepository` 共 25 处 `Log.d`（含耗时埋点），release 下仍会输出。**修复建议**：接入 `BuildConfig.DEBUG` 条件或 `Timber` 按环境控制日志级别。
5. **UI 文案硬编码中文** — 界面大量中文直接写在代码中，未抽到 `strings.xml`，无法做多语言。**修复建议**：如未来需国际化，将 `ui/*.kt` 中的文案抽离。
6. **操作记录无上限** — 记录存为单个 JSON 字符串，无条数上限与清理策略，长期使用会无限增长。**修复建议**：限制最大条数（如 500），超出时裁剪最旧记录。
7. **占位与隐藏功能** — 「清单库」跳转占位网址 `https://github.com`；搜索框 `showSearchBar = false` 硬编码隐藏；`data_extraction_rules.xml` 留有模板 TODO。**修复建议**：明确这些功能去留，删除或实现。
8. **文档与代码脱节** — 《阴阳门使用说明.md》仍描述已移除的「规则库」设置项，TODO.md 引用的 `AdbWirelessClient` 类不存在。**修复建议**：按当前代码更新文档，或将 TODO 拆分为「占位类尚未创建」明确标注。
