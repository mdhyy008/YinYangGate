# 阴阳门 · 软件说明书

> 版本：v1.0（build 1）　|　包名：`cn.mdhyy.yinyanggate`
> 编写依据：源码实际行为（2026-08-14 最新代码）　|　应用冻结/解冻管理工具

---



---

## 一、软件简介

「阴阳门」是一款 Android 应用**冻结 / 解冻**管理工具，灵感取自「阴阳」二元思想：

- **阳面**：正常可用的应用（未冻结），明亮主题。
- **阴面**：已冻结的应用（被隐藏、无法运行），暗色主题。

两个面通过 **180° 翻转动画**切换，像翻动硬币正反面一样管理应用。冻结走系统「停用应用」机制（`pm disable-user`），不删除任何应用数据，可随时解冻恢复。

本说明书依据当前源码的实际行为编写，覆盖软件**每一个界面**与**每一个功能**，与代码保持一致。

> 注：另有面向用户的《阴阳门使用说明.md》（v1.0），本文是其补充与更新版，以最新代码行为为准。

---

## 二、界面总览与导航结构

软件共有 **3 个界面（页面）**，通过应用内导航切换：

```
┌─────────────────────────────────────────┐
│  主界面（MAIN）  ← 应用启动后的默认页       │
│  ├── 顶栏：操作记录 ─ 面切换按钮 ─ 设置      │
│  ├── 应用网格（阳面 / 阴面）                │
│  ├── （可选）底部切换按钮                    │
│  ├── （可选）多选批量操作栏                 │
│  └── （弹出）功能菜单、各类对话框            │
├─────────────────────────────────────────┤
│  操作记录页（HISTORY）  ← 点击顶栏时钟进入   │
│  顶栏：返回 ─ 标题「操作记录」               │
├─────────────────────────────────────────┤
│  设置页（CHANNELS）  ← 点击顶栏齿轮进入     │
│  顶栏：返回 ─ 标题「设置」                  │
│  通用 → 权限模式 → 关于                     │
└─────────────────────────────────────────┘
```

**界面间跳转方式：**

| 从 | 操作 | 到达 |
|---|---|---|
| 主界面 | 点击顶栏**左上角时钟图标** | 操作记录页 |
| 主界面 | 点击顶栏**右上角齿轮图标** | 设置页 |
| 操作记录页 / 设置页 | 点击**返回按钮** 或 **系统返回键** | 主界面 |
| 主界面（非多选） | **系统返回键** | 弹出「退出应用」确认框 |
| 主界面（多选模式） | **系统返回键** | 先退出多选模式（不退出应用） |

页面切换带横向滑动动画：历史页从左侧滑入、设置页从右侧滑入，返回时反向。

---

## 三、主界面详解

主界面是软件的核心，从上到下依次为：**顶栏 → 进度条（可选）→ 应用网格 → 底部元素**。

### 3.1 顶栏（YinYangTopBar）

| 位置 | 元素 | 作用 |
|---|---|---|
| 左上角 | 时钟图标 | 进入操作记录页 |
| 中间 | 面切换按钮 或 标题 | 显示当前面及数量（如「阳面 · 270」），点击触发翻转；见 3.4 |
| 右上角 | 齿轮图标 | 进入设置页 |

> 当设置里开启「切换按钮显示在屏幕下方」时，顶栏中间只显示「阴阳门」标题，面切换按钮移到屏幕底部。

### 3.2 面切换按钮（FaceSwitchButton）

- 由 **明暗图标 + 文字** 组成，文字形如 `阳面 · N` 或 `阴面 · N`（N 为当前面应用数）。
- **单击**：执行 180° 翻转动画切换阴阳面。
- **长按**：弹出功能菜单（见第十一节）。
- 中间按钮文字带淡入淡出切换动画。

### 3.3 应用网格（AppGrid）

- 应用以**网格**展示，图标 + 名称，按名称**拼音字母排序**。
- 网格**自适应列宽**（`GridCells.Adaptive(84.dp)`），横竖屏均良好展示。
- **阳面**只显示未冻结应用；**阴面**只显示已冻结应用。
- 每个应用卡片：
  - **右上角红色小圆点** → 系统应用，操作需谨慎。
  - 名称超长自动省略号。
  - **单击**：单选操作（见 3.5）。
  - **长按**：进入多选模式（见第八节）。
- 某面为空时显示「该面暂无应用」。
- 已设置「显示系统应用」关闭时，列表过滤掉系统应用（默认隐藏）。

### 3.4 阴阳面切换（180° 翻转动画）

**3 种切换方式：**

1. **点击顶栏（或底部）面切换按钮**：立即触发 900ms 的 180° 翻转动画。
2. **左右滑动应用列表**（需在设置开启「左右滑动切换面」）：在列表上左右快速滑动触发，触发距离约屏幕宽度的 **20%**，滑动中实时翻转。
3. 翻转至 **90°** 时完成换面。

**翻转的视觉效果：**
- 网格通过 `rotationY` 做三维旋转，带视差景深（`cameraDistance`）。
- **整个界面的主题颜色**随翻转进度**线性插值渐变**：阳面为白底黑字（明亮），阴面为黑底浅字（暗色），连卡片、按钮、错误色都连续过渡。
- 每次只挂载当前可见的一面网格，避免两面同时渲染。

**记忆行为：** 当前所处面会被持久保存，重启应用保持。

### 3.5 单个应用冻结 / 解冻

1. 在阳面/阴面**单击**应用卡片。
2. 弹出确认对话框，显示应用名、包名、将使用的通道（如「将通过 Shizuku 冻结」）：
   - **冻结**提示：「冻结后应用将从桌面消失、无法运行，可随时在阴面解冻恢复。」
   - **系统应用**额外显示红色警告：「该应用为系统应用，冻结可能导致系统异常或其它应用无法工作！」
   - **解冻**提示：「解冻后应用将恢复正常可用。」
3. 点「确认冻结 / 确认解冻」执行，点「取消」关闭。
4. 成功后顶部提示条显示「已完成 1 个 · {通道名}」。

> **例外**：当所选通道是 **ADB 半手动**时，单击应用**不弹确认框**，直接弹出 ADB 命令引导对话框（见第九节）。

### 3.6 顶部提示条（自绘 Toast）

- 操作结果通过**屏幕顶部居中**的黑色圆角提示条显示，1.8 秒后自动消失。
- 位于状态栏下方，不遮挡底部按钮，也不受系统 Toast 位置限制。
- 连续相同文案也会重新计时显示。

### 3.7 底部面切换按钮（FaceSwitchOverlay）

当设置开启「切换按钮显示在屏幕下方」时，屏幕底部显示悬浮切换按钮：

- 具备明暗**两种阴影色**，保证阳面/阴面下都清晰可见。
- **向下滚动**应用网格超过两行时按钮自动隐藏，向上滚动立即显示。
- 多选模式或处理中自动隐藏。
- 长按同样弹出功能菜单。

### 3.8 处理中进度

- 执行冻结/解冻时，顶栏下方显示一条 2dp 的**线性进度条**。
- 多选批量时，底部操作栏显示「正在处理 N/M」实时计数。

### 3.9 搜索（隐藏功能）

- 界面内已实现搜索栏（按名称/包名过滤），但当前**硬编码隐藏**（`showSearchBar = false`），需要时把该开关改为 `true` 即可恢复显示。

### 3.10 退出确认

主界面（非多选状态）按**系统返回键**，弹出「退出应用」确认框：

- 「退出」→ 关闭应用；「取消」→ 继续使用。

### 3.11 旋转屏幕

- 已在 `AndroidManifest.xml` 声明 `configChanges`，旋转屏幕**不会重新加载**应用列表，保持当前界面、数据与所处面状态。

---

## 四、操作记录页详解

点击主界面顶栏**左上角时钟图标**进入。

- 完整记录所有**冻结 / 解冻**操作，一条记录包含：
  - **应用图标**：应用名首字圆形色块（无真实图标）。
  - **应用名称**。
  - **时间**：`MM-dd HH:mm` 格式。
  - **通道**：使用的权限通道名。
  - **动作徽标**：`冻结`（蓝色）或 `解冻`（绿色）圆角标签。
- 记录按时间**倒序**排列，最新的在最上面。
- 状态：
  - 加载中 → 居中显示「加载中…」。
  - 无记录 → 居中显示「暂无操作记录」。
- 顶栏带返回按钮；系统返回键也返回主界面。
- 记录**持久保存**，重启不丢失。

---

## 五、设置页详解

点击主界面顶栏**右上角齿轮图标**进入。页面自上而下分 3 个区域。

### 5.1 通用

| 设置项 | 说明 |
|---|---|
| **左右滑动切换面** | 开关。开启后在主界面应用列表上左右滑动即可切换阳面/阴面 |
| **显示系统应用** | 开关。开启后列表显示系统应用（默认关闭） |
| **切换按钮显示在屏幕下方** | 开关。开启后面切换按钮移到屏幕底部悬浮，顶栏只留标题 |

### 5.2 权限模式

列出 5 个权限通道（详见第六节）：

1. Root 权限
2. Shizuku
3. 设备所有者（DPM）
4. 手动 ADB 模式
5. 电脑客户端

每条通道行显示：图标、名称、状态描述、右侧操作按钮。**点击通道行即选中**，对勾（✓）移动到该行并立即生效。

- 通道名右侧有 **问号小图标**，点击弹出该通道的「运行原理 / 使用方法」帮助弹窗。
- DPM 通道帮助弹窗内含「复制命令」按钮，可一键复制 `dpm set-device-owner` 命令。

### 5.3 关于

| 设置项 | 说明 |
|---|---|
| **帮助机器人** | 点击跳转在线帮助中心（浏览器打开 `https://help.web.mdhyy.cn/?doc=yinyang_help`） |
| **关于阴阳门** | 显示「阴阳门 v{版本号} · 应用冻结/解冻管理工具」 |

> 帮助链接为空时点击提示「帮助链接待配置」。

---

## 六、权限通道功能详解

冻结/解冻属于受限系统操作，需系统级权限。软件提供 **5 种通道**，选择哪个就用哪个，**不会自动回退**。通道选择会**持久记忆**，重启保留。

### 6.1 Root 通道

- **可用判定**：执行 `su -c id`，输出含 `uid=0` 即已 Root 可用。
- **冻结**：`su -c pm disable-user --user 0 <包名>`
- **解冻**：`su -c pm enable <包名>`
- **设置页显示**：已就绪 / 未检测到 Root。

### 6.2 Shizuku 通道（推荐）

通过 Shizuku 服务借用 adb/root 权限，无 Root 也可用。

- **可用判定**：`Shizuku.pingBinder()` 且已授权。
- **冻结**：`pm disable-user --user 0 <包名>`
- **解冻**：`pm enable <包名>`
- **设置页状态**：未安装 / 未运行 / 未授权 / 已授权。
  - **未安装** → 显示「安装 Shizuku」，点击跳应用商店。
  - **已装未运行** → 显示「打开 Shizuku」，点击启动服务。
  - **服务已运行未授权** → 显示「授权」，点击弹出系统授权窗。
- **授权成功后自动选中 Shizuku 通道**，并提示「Shizuku 授权成功」；拒绝则提示「Shizuku 授权被拒绝」。
- 兼容包名：官方 `moe.shizuku.xyz` 与分发版 `moe.shizuku.privileged.api`。

### 6.3 设备所有者（DPM）通道

将本应用设为设备所有者后，直接调用系统 API 静默冻结/解冻。

- **可用判定**：`DevicePolicyManager.isDeviceOwnerApp`。
- **冻结**：`setApplicationHidden(admin, pkg, true)`；**解冻**：`setApplicationHidden(admin, pkg, false)`。静默、无提示。
- **开启方式**（需电脑 + adb）：
  ```bash
  adb shell dpm set-device-owner cn.mdhyy.yinyanggate/.admin.AdminReceiver
  ```
- **⚠️ 重要警告**：设置设备所有者会**清空手机全部数据**（近似恢复出厂），且难以解除。门槛高，非必要不建议。
- **设置页显示**：已启用 / 未启用。

### 6.4 ADB 半手动通道

无 Root、不装 Shizuku 时，通过 USB 调试配合电脑手动执行命令。

- **可用判定**：`Settings.Global.ADB_ENABLED == 1`（USB 调试已开启）。
- 未开启时显示「开启调试」，点击跳转系统开发者选项。
- **与其它通道不同**：此通道下冻结/解冻**不自动完成**，弹 ADB 命令引导对话框，由用户在电脑上执行（详见第九节）。
- **设置页显示**：已开启调试 / 未开启调试。

### 6.5 电脑客户端通道

通过局域网与电脑端「阴阳门电脑端」通信，由电脑执行 adb 命令。

- **可用判定**：`PcClientManager.isConnected`（已连上电脑）。
- **选中该通道时自动开始查找电脑**；状态显示：未连接电脑 / 正在查找电脑… / 已连接 {电脑名} ({IP}) / 未找到电脑（含失败原因）。
- 冻结/解冻指令经 UDP 发给电脑，由电脑执行 `pm` 并回传结果（详见第十二节）。
- 手机端通信代码已完成，**需配套 Windows 电脑客户端**才能闭环（电脑端尚在规划）。

### 6.6 通道选择规则

- 点击通道行即选中、立即生效，对勾高亮，持久记忆。
- **不会自动回退**：选中的通道不可用，操作直接报错提示。
- 默认通道：已安装 Shizuku 则默认 Shizuku，否则默认 ADB。

---

## 七、冻结 / 解冻功能详解

### 7.1 执行流程（单应用）

1. 用户确认操作后，在后台协程执行（主线程不卡顿）。
2. 按选中通道执行冻结/解冻命令。
3. 成功后**轮询等待状态生效**：每 200ms 检测一次，最多 25 次（约 5 秒），避免图标迟迟不消失/进度秒退。
4. 写一条操作记录。
5. 全部执行完再刷新列表并提示「已完成 N 个 · {通道}」。
6. 若需要手动命令（ADB 通道），弹 ADB 引导框；出现错误则提示「失败：{原因}」。

### 7.2 执行流程（批量）

- 逐条执行，实时计数进度；批量执行完提示「已完成 N/M 个 · {通道}」。
- 单个失败不中断整体，提示失败应用名。

### 7.3 状态判定

- 冻结状态 = `getApplicationEnabledSetting` 返回**不是** DEFAULT 且**不是** ENABLED（即 DISABLED_USER 等停用态）。
- 冻结命令作用于**用户 0**（主用户）。

### 7.4 自动过滤

- 应用列表自动排除本应用自身（阴阳门自己），防止误冻导致软件失效。

---

## 八、批量操作与多选功能详解

### 8.1 进入与选择

1. **长按**任意应用卡片进入多选模式，被长按的应用自动选中。
2. 继续点击其它应用**切换选中**，选中的卡片有**高亮边框**。
3. 多选模式下顶栏/网格正常展示，底部出现批量操作栏。

### 8.2 批量操作栏（BatchActionBar）

显示「已选 N 个」与主按钮，按钮文案智能判定：

| 选中状态 | 主按钮文案 |
|---|---|
| 全部未冻结 | 「冻结 N 个」 |
| 全部已冻结 | 「解冻 N 个」 |
| 混合状态 | 「切换状态 N 个」（逐个翻转） |

栏内控件：

- **全选 / 反选按钮**：仅**阴面**可全选；当前面已全部选中时同一按钮变为**反选**（取消全选）。
- **更多菜单（⋮）**：内含「**导出应用清单规则**」（见第十节）。
- **执行按钮**：点击弹出二次确认框（显示通道、操作数量；含系统应用且有冻结时红色警告），确认后执行。

### 8.3 多选模式下的返回键

- 系统返回键**先退出多选**（取消选择），不直接退出应用。

### 8.4 批量执行

- 与单应用一致的轮询等待生效 + 记录 + 刷新提示流程。
- 执行完成后自动退出多选模式。

---

## 九、ADB 半手动模式详解

当选中 **ADB 通道** 执行冻结/解冻时（单应用点击或批量操作），弹出「ADB 手动执行」对话框：

1. 列出需要你在电脑上执行的**命令列表**：
   - 冻结：`adb shell pm disable-user --user 0 <包名>`
   - 解冻：`adb shell pm enable <包名>`
2. 每条命令以**深色代码块**展示（亮/暗主题下都清晰），右侧有**复制按钮**。
3. 底部「**复制全部**」一次性复制所有命令。
4. 在电脑上执行完成后，回应用点「**我已执行**」。
5. 应用自动**检测冻结状态**：最多重试 3 次（每次间隔 300ms），检测后弹出「执行结果检测」：
   - 每个应用显示「已被冻结 / 未冻结」，成功数统计「成功 N/M 个」。
   - 同时刷新列表。
6. 点「关闭」可放弃验证（不写记录）。

---

## 十、规则清单功能详解

### 10.1 导出规则

在多选模式下，通过批量栏「⋮ → 导出应用清单规则」，将选中的应用导出为 JSON 并复制到**剪贴板**：

```json
{
  "type": "yinyang_rules",
  "version": 1,
  "apps": [
    { "package": "com.xxx.abc", "name": "应用名" }
  ]
}
```

提示「已导出 N 条应用规则到剪贴板」。

### 10.2 导入批处理（清单批处理）

1. 长按顶栏/底部面按钮打开**功能菜单**，点「**清单批处理**」。
2. 应用读取**剪贴板**中的 `yinyang_rules` JSON；格式无效时提示「剪贴板中没有有效清单」。
3. 有效则弹出「清单批处理」对话框：显示当前通道、应用数量与清单列表。
4. 点「**冻结**」或「**解冻**」批量执行（带二次确认与系统应用警告）。
5. 失败应用逐个提示；需要手动命令时同样走 ADB 引导。

### 10.3 规则库（占位）

功能菜单中的「清单库」当前为占位入口，跳转占位网址（`https://github.com`），后续规划为云端规则库/分享。

---

## 十一、功能菜单详解

**打开方式**：长按顶栏中间的**面切换按钮**（或底部悬浮切换按钮，若开启置底模式）。

- 弹出从按钮中心飞向屏幕中心的**模拟 Dock 弹出动画**（缩放 + 位移 + 弹簧回弹 + 半透明遮罩）。
- 遮罩点击空白处或「关闭」退出。

**菜单内容：**

| 元素 | 功能 |
|---|---|
| 搜索框 | 在菜单内搜索应用（按名称/包名） |
| 搜索列表 | 点击某应用 → 直接弹该应用的冻结/解冻确认框（快速操作入口） |
| **清单批处理** | 从剪贴板导入规则清单并批量冻结/解冻 |
| **清单库** | 占位，跳转外部网页 |
| **手动刷新** | 重新加载应用列表，完成后提示「刷新完成」 |

> 菜单搜索功能让「想冻某个应用但不在当前列表」时快速定位，无需翻面查找。

---

## 十二、电脑客户端通信功能详解

> 手机端代码已实现；需配套 Windows 电脑客户端（规划中）才能闭环。

### 12.1 通信方式

- **协议**：UDP 数据报，消息体为 JSON 字符串（UTF-8）。
- **固定端口**：手机绑定 **45678**，电脑监听 **45679**。

### 12.2 连接流程

1. 选中「电脑客户端」通道 → 自动广播 `DISCOVER`（含手机型号名）到 `255.255.255.255:45679`。
2. 电脑收到后回复 `DISCOVER_REPLY`（含电脑名 `win`、源端口）。
3. 手机收到回复 → 状态变「已连接」，**自动推送应用列表**给电脑。
4. 3 秒未找到 → 状态「未找到电脑」。
5. 也支持手动 IP 单播直连兜底。

### 12.3 应用列表推送

连接成功后自动推送；电脑发 `REQUEST_LIST` 时重新推送：

- `APP_LIST_START`：总数 + 冻结数。
- `APP_LIST_FRAG`：分片数据，每片 ≤ 1200 字节，每项含包名(`p`)、名称(`n`)、是否系统(`s`)、是否冻结(`f`)、**图标色块颜色**(`c`，按名称首字从 14 色调色板稳定生成)。
- `APP_LIST_END`：结束标记。

### 12.4 冻结指令中继

1. 手机发 `FREEZE`：包名 `pkg` + 动作 `action`(disable/enable) + 名称 `label` + **`androidId`**。
2. 电脑执行 adb 命令后回 `FREEZE_RESULT`：`ok`(成功与否) + `msg`。
3. 手机**等待最多 15 秒**回执；超时提示「等待电脑响应超时」。
4. 通过包名|动作作为 key 关联挂起请求。

### 12.5 已实现的手机端协议消息总表

| 方向 | 消息类型 | 用途 |
|---|---|---|
| 手机→电脑 | `DISCOVER` | 广播查找电脑 |
| 手机→电脑 | `APP_LIST_START` / `APP_LIST_FRAG` / `APP_LIST_END` | 推送应用列表 |
| 手机→电脑 | `FREEZE` | 请求电脑执行冻结/解冻 |
| 电脑→手机 | `DISCOVER_REPLY` | 回应查找（含电脑名） |
| 电脑→手机 | `REQUEST_LIST` | 请求重新推送列表 |
| 电脑→手机 | `FREEZE_RESULT` | 冻结/解冻执行结果 |

> **已知风险**：UDP 明文、无鉴权加密。局域网内任何设备回复 `DISCOVER_REPLY` 即可伪装成电脑，进而拉取应用列表、接收 `androidId`。正式使用前需引入配对与加密（见 TODO-电脑客户端.md 的「鉴权/安全」待定项）。

---

## 十三、应用列表快照缓存（内部机制）

为加快启动速度、避免每次进应用全量扫描，软件实现了快照缓存：

1. **首次加载**：全量扫描已安装应用 → 渲染图标为封顶 128px 位图 → 图标写入 `filesDir/snapshot/icons/{包名}.png` → 元数据写入 `snapshot/snapshot.json`。
2. **再次启动**：
   - 有快照 → **先显示快照**（图标来自缓存）快速上屏，不转圈。
   - 已安装应用集合与快照集合不同（安装/卸载过）→ **全量刷新**并更新快照。
   - 集合相同 → **轻量校正**：仅逐个刷新冻结状态，不重载 label/icon。
3. 清理逻辑：删除已卸载应用的孤儿图标。
4. `snapshot.json` 含版本号、时间戳、应用数组（包名/名称/是否系统/是否冻结）。

> 该机制对外表现为「启动很快、列表秒出」，无需用户感知或操作。

---

## 十四、操作记录存储与持久化

- 存储位置：`SharedPreferences`（文件名 `yinyang_gate`）。
- 记录格式：JSON 数组，每项含包名、名称、时间戳、通道、动作（"冻结"/"解冻"）。
- 读取时按时间倒序排列展示。
- **无条数上限**（当前），长期使用会持续增长。

---

## 十五、设置项总表

| 设置项 | 存储 Key | 位置 | 说明 |
|---|---|---|---|
| 当前面（阳/阴） | `current_face_dark` | 主界面 | 翻转后自动保存，重启保持 |
| 左右滑动切换面 | `swipe_to_switch` | 设置→通用 | 默认关 |
| 显示系统应用 | `show_system_apps` | 设置→通用 | 默认关 |
| 切换按钮置底 | `switch_button_bottom` | 设置→通用 | 默认关 |
| 选中权限通道 | `preferred_channel` | 设置→权限模式 | 默认 Shizuku 或 ADB |
| 操作记录 | `freeze_history` | 数据层 | JSON 数组，无上限 |

---

## 十六、常见问题（FAQ）

**Q1：点击冻结提示「无可用通道，请先开启 root / Shizuku / ADB 之一」？**
答：到设置→权限模式，确认选中的通道是否可用。软件不做自动回退，选了哪个通道就用哪个，不可用则操作失败。

**Q2：Shizuku 显示「未授权」怎么处理？**
答：确认 Shizuku 服务已运行（可点「打开 Shizuku」），再点通道行「授权」，在系统窗口点允许；授权成功会自动选中 Shizuku。

**Q3：用 ADB 通道冻结没生效？**
答：ADB 是半手动通道，需在电脑上执行它给出的命令后，回应用点「我已执行」才会生效。

**Q4：冻结后应用图标不见了？**
答：正常。冻结的应用从桌面消失、无法运行，切到阴面即可看到并解冻恢复。

**Q5：误冻系统应用导致异常？**
答：尽快到阴面解冻，或用电脑 `adb shell pm enable <包名>` 恢复。

**Q6：旋转屏幕会不会重载？**
答：不会，已声明 `configChanges`，旋转保持界面与数据、面状态。

**Q7：操作记录会丢吗？**
答：不会，持久保存，重启不丢。冻结与解冻都会记录。

**Q8：电脑客户端通道连不上？**
答：需手机与电脑同一局域网、电脑端「阴阳门电脑端」已运行且 adb 就绪；选通道会自动查找，也可用 IP 直连兜底。

**Q9：功能菜单怎么打开？**
答：长按顶栏（或底部）的面切换按钮。

---

## 十七、注意事项与风险提示

1. **冻结系统应用有风险**：可能导致系统异常、其它应用无法工作甚至无法开机。界面会红色警告，请谨慎。
2. **DPM 设备所有者代价极高**：`dpm set-device-owner` 会**清空设备全部数据**且难解除，非必要不建议。
3. **ADB 通道需手动执行**：以应用给出的命令为准。
4. **请勿冻结阴阳门自身**：应用列表已自动过滤本应用。
5. **数据安全**：冻结不删除任何应用数据，解冻后完整恢复。
6. **多用户**：冻结作用于用户 0（主用户），多用户/分身场景以系统实际表现为准。
7. **电脑客户端协议未加密**：局域网内存在被伪装连入的风险，正式使用前需解决鉴权。
8. **文档一致性**：本说明书依据 2026-08-14 源码编写；旧版《阴阳门使用说明.md》中关于设置页「规则库」占位的描述已过时，以本说明书为准。
